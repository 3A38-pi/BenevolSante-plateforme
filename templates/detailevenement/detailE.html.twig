{% extends 'base.html.twig' %}

{% block title %}Hello EvenementController!{% endblock %}

{% block body %}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails de l'Événement</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Effet fade-in pour l'affichage des détails */
        .fade-in {
            animation: fadeIn 1s ease-out forwards;
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Effet de survol pour les boutons */
        .btn {
            transition: transform 0.2s ease, background-color 0.2s ease;
        }

        .btn:hover {
            transform: scale(1.1);
            background-color: #28a745;
            color: white;
        }

        /* Section de description de l'événement */
        .event-description {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        /* Décoration de l'en-tête */
        .event-header {
            text-align: center;
            margin-bottom: 30px;
        }

        .event-header h1 {
            font-size: 2.5rem;
            color: #28a745;
        }

        .event-header p {
            font-size: 1.2rem;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="container mt-5 fade-in">
        <!-- En-tête de l'événement -->
        <div class="event-header">
            <h1>Journée de la Santé</h1>
            <p><strong>Date :</strong> 2025-03-15 | <strong>Lieu :</strong> Hôpital Central</p>
        </div>

        <!-- Description de l'événement -->
        <div class="event-description">
            <h3>Description de l'événement</h3>
            <p>
                La Journée de la Santé est un événement destiné à sensibiliser le public sur l'importance de la santé.
                Des experts médicaux interviendront pour discuter de sujets variés allant de la prévention des maladies à
                des conseils pour améliorer le bien-être au quotidien. Des consultations gratuites seront proposées pour les
                participants, ainsi que des ateliers interactifs.
            </p>
        </div>

        <!-- Détails supplémentaires -->
        <div class="mt-4">
            <h4>Objectifs de l'événement :</h4>
            <ul>
                <li>Informer le public sur les maladies courantes et les moyens de prévention.</li>
                <li>Offrir des consultations médicales gratuites à ceux dans le besoin.</li>
                <li>Fournir des conseils pratiques pour un mode de vie plus sain.</li>
            </ul>
        </div>

        <!-- Boutons d'action -->
        <div class="mt-5">
            <a href="{{path('app_evenement')}}" class="btn btn-secondary">Retour à la liste</a>
            <a href="modifier_evenement.html" class="btn btn-warning">Modifier l'événement</a>
            <a href="#" class="btn btn-danger">Supprimer l'événement</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
{% endblock %}
