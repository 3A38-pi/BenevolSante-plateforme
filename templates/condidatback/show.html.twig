{% extends 'base2.html.twig' %}

{% block title %}Détail du Candidat{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center text-primary mb-4">👤 Détail du Candidat</h1>
        
        <div class="card shadow-lg p-4 mb-4 bg-light animate__animated animate__fadeInUp">
            <div class="card-body">
                <h4 class="card-title text-primary"><i class="fas fa-user"></i> {{ condidat.nom }} {{ condidat.prenom }}</h4>
                <p class="card-text"><strong>🆔 ID:</strong> {{ condidat.id }}</p>
                <p class="card-text"><strong>📧 Email:</strong> {{ condidat.email }}</p>
                <p class="card-text"><strong>📞 Téléphone:</strong> {{ condidat.telephone }}</p>
                <p class="card-text"><strong>📄 CV:</strong> 
                    <a href="{{ asset('uploads/cv/' ~ condidat.cv) }}" target="_blank" class="btn btn-info btn-sm">
                        📄 Voir CV
                    </a>
                </p>
            </div>
        </div>
        
        <div class="d-flex justify-content-between">
            <a href="{{ path('app_condidatback_index') }}" class="btn btn-secondary shadow">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
            <div>
                <a href="{{ path('app_condidatback_edit', {'id': condidat.id}) }}" class="btn btn-warning shadow">
                    <i class="fas fa-edit"></i> Modifier
                </a>
                <form method="post" action="{{ path('app_condidatback_delete', {'id': condidat.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce candidat ?');" class="d-inline">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ condidat.id) }}">
                    <button type="submit" class="btn btn-danger shadow">
                        <i class="fas fa-trash-alt"></i> Supprimer
                    </button>
                </form>
            </div>
        </div>
    </div>

    <!-- Ajout d'animations et styles supplémentaires -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
{% endblock %}
