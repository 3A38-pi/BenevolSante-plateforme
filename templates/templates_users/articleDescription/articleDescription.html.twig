{% extends 'base.html.twig' %}

{% block Navbar %}
  <a><button href="javascript:history.back()"  class="back-btn">Retour</button></a>
{% endblock %}



{% block body %}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page de Description</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 0;
            background-color: #f8f9fa;
        }
        .container-xxl {
            max-width: 1000px;
            margin: auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: left;
        }
        .article-image {
            width: 100%;
            border-radius: 8px;
        }
        .description {
            margin-top: 15px;
            font-size: 16px;
            color: #333;
        }
        .divider {
            height: 2px;
            background: #ccc;
            margin: 20px 0;
        }
        .comments-section {
            max-width: 100%;
            margin: auto;
        }
        .comment {
            border-bottom: 1px solid #ddd;
            padding: 10px;
        }
        .reply-btn {
            color: blue;
            cursor: pointer;
            font-size: 14px;
        }
        .reply-box {
            display: none;
            margin-top: 10px;
        }
        textarea {
            width: 100%;
            padding: 10px;
            font-size: 14px;
            border: 1px solid #ccc;
            border-radius: 5px;
            resize: none;
            transition: border-color 0.3s;
        }
        textarea:focus {
            border-color: #007bff !important;
            outline: none;
        }
        .submit-btn {
            display: block;
            width: 100%;
            margin-top: 10px;
            padding: 10px;
            background: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .submit-btn:hover {
            background: #0056b3;
        }
    </style>
</head>
<body>

<div class="container-xxl py-5">
    <h1>Demo post with formatted elements and comments.</h1>
    
    <div class="article-image-container">
        <img src="{{ asset('/img/photos.jpg') }}" class="article-image" alt="Article Image">
    </div>
    
    <p class="description">
        Ceci est une description de l'article. Elle donne des détails et des informations intéressantes 
        pour mieux comprendre le sujet présenté.
    </p>

    <div class="divider"></div>

    <div class="comments-section">
        <h3>3 commentaires</h3>
        <div class="comment">
            <strong>Utilisateur 1</strong>
            <p>Test</p>
            <span class="reply-btn" onclick="showReplyBox(this)">Reply</span>
            <div class="reply-box">
                <textarea rows="3" placeholder="Écrire une réponse..."></textarea>
                <button class="submit-btn">Envoyer</button>
            </div>
        </div>
        <div class="comment">
            <strong>Utilisateur 2</strong>
            <p>Second test</p>
            <span class="reply-btn" onclick="showReplyBox(this)">Reply</span>
            <div class="reply-box">
                <textarea rows="3" placeholder="Écrire une réponse..."></textarea>
                <button class="submit-btn">Envoyer</button>
            </div>
        </div>
    </div>

   <br>

    <div class="add-comment">
        <h3>Ajouter un commentaire</h3>
        <textarea rows="4" placeholder="Écrire un commentaire..."></textarea>
        <button class="submit-btn">Poster</button>
    </div>
</div>

<script>
    function showReplyBox(element) {
        let replyBox = element.nextElementSibling;
        replyBox.style.display = replyBox.style.display === "block" ? "none" : "block";
    }
</script>

</body>
</html>
{% endblock %}


