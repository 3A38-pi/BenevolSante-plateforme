{% extends 'base2.html.twig' %}

{% block title %}Edit Participant{% endblock %}

{% block body %}
    <h1>Edit Participant</h1>

    {{ include('participantback/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_participantback_index') }}">back to list</a>

    {{ include('participantback/_delete_form.html.twig') }}
{% endblock %}
