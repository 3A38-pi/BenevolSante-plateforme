{% extends 'base2.html.twig' %}

{% block title %}Détails de la Catégorie{% endblock %}

{% block body %}
<div class="container mt-5">
    <!-- Titre animé -->
    <div class="text-center mb-4">
        <h1 class="text-primary animate__animated animate__fadeInDown">📂 Détails de la Catégorie</h1>
        <p class="text-muted">Voici les informations de la catégorie sélectionnée.</p>
    </div>

    <!-- Carte des détails -->
    <div class="card shadow-lg p-4 animate__animated animate__zoomIn">
        <div class="card-body">
            <h2 class="card-title text-center text-dark mb-3">📌 Informations</h2>

            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th scope="row" class="bg-light">🆔 ID</th>
                        <td>{{ categorie.id }}</td>
                    </tr>
                    <tr>
                        <th scope="row" class="bg-light">🏷️ Type</th>
                        <td>{{ categorie.type }}</td>
                    </tr>
                </tbody>
            </table>

            <!-- Boutons d'action -->
            <div class="text-center mt-4">
                <a href="{{ path('app_categorie_index') }}" class="btn btn-outline-secondary btn-lg">
                    ⬅ Retour à la Liste
                </a>
                <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-warning btn-lg">
                    ✏️ Modifier
                </a>
                {{ include('categorie/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>

<!-- Ajout de styles et animations -->
<style>
    /* Effet d'ombre et d'animation pour la carte */
    .card {
        border-radius: 12px;
        transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
    }

    .card:hover {
        transform: scale(1.02);
        box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.1);
    }

    /* Animation des boutons */
    .btn {
        transition: all 0.3s ease-in-out;
    }

    .btn:hover {
        transform: scale(1.1);
    }
</style>

<!-- Ajout de la bibliothèque Animate.css -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">

{% endblock %}
