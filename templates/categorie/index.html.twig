{% extends 'base2.html.twig' %}

{% block title %}Catégorie Index{% endblock %}

{% block body %}
<div class="container mt-5">
    <!-- Titre animé -->
    <h1 class="text-center text-primary animate__animated animate__fadeInDown">📂 Liste des Catégories</h1>

    <!-- Table avec animations et style Bootstrap -->
    <div class="table-responsive animate__animated animate__zoomIn animate__faster">
        <table class="table table-hover table-bordered shadow mt-4">
            <thead class="table-primary text-center">
                <tr>
                    <th>ID</th>
                    <th>Type</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for categorie in categories %}
                <tr class="animate__animated animate__fadeInUp animate__delay-0.5s">
                    <td class="text-center fw-bold">{{ categorie.id }}</td>
                    <td>{{ categorie.type }}</td>
                    <td class="text-center">
                        <a href="{{ path('app_categorie_show', {'id': categorie.id}) }}" class="btn btn-info btn-sm hvr-grow">
                            <i class="fas fa-eye"></i> Voir
                        </a>
                        <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-warning btn-sm hvr-grow">
                            <i class="fas fa-edit"></i> Modifier
                        </a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3" class="text-center text-muted animate__animated animate__fadeIn">Aucune catégorie trouvée</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <!-- Bouton Create New avec effet hover -->
    <div class="text-center mt-4">
        <a href="{{ path('app_categorie_new') }}" class="btn btn-success btn-lg animate__animated animate__bounceIn hvr-bounce-to-right">
            <i class="fas fa-plus-circle"></i> Ajouter une Catégorie
        </a>
    </div>
</div>

<!-- Ajout de Bootstrap, Animate.css, Hover.css et FontAwesome -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/hover.css/2.3.1/css/hover-min.css">
<script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

{% endblock %}
