{% extends 'base2.html.twig' %}

{% block title %}Offre{% endblock %}

{% block body %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Titre_offre</th>
                <td>{{ offre.titreOffre }}</td>
            </tr>
            <tr>
                <th>Description_offre</th>
                <td>{{ offre.descriptionOffre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offreback_index') }}">back to list</a>

    <a href="{{ path('app_offreback_edit', {'id': offre.id}) }}">edit</a>

    {{ include('offreback/_delete_form.html.twig') }}
{% endblock %}
